/**
 * Shared constants for AI providers
 */

/**
 * Default temperature for content generation
 * Controls randomness in the output (0.0 = deterministic, 1.0 = very random)
 */
export const DEFAULT_TEMPERATURE = 0.8;
